# Optional: call the platform as the premium service account (Keycloak client
# clairwave-mcp, client-credentials grant). Without these the server runs anonymously.
CW_MCP_CLIENT_ID=clairwave-mcp
CW_MCP_CLIENT_SECRET=
# CW_KC_TOKEN_URL=https://www.clairwave.com/auth/realms/clairwave/protocol/openid-connect/token
# CW_MCP_LOG=/path/to/analytics.jsonl
# MCP_PORT=8890
